void delay_us(unsigned int);   // Function prototype for microsecond delay (delays execution by specified microseconds)
void delay_ms(unsigned int);   // Function prototype for millisecond delay (delays execution by specified milliseconds)
void delay_s(unsigned int);    // Function prototype for second delay (delays execution by specified seconds)

